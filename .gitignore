node_modules/
dist/
# Built binaries and the skills/ copy (npm-installed deps) — regenerated by
# scripts/build-binaries.sh, not meant to be tracked. dist-bin/README.md and
# dist-bin/env.example are hand-maintained/template files and stay tracked
# (a past dist-bin/ blanket-ignore + a careless `rm -rf dist-bin` in the
# build script once deleted dist-bin/README.md permanently, since it was
# never in git — not repeating that).
dist-bin/macos-arm64/
dist-bin/windows-x64/
dist-bin/skills/
bundle/
.env
sessions/
.core-agent/
*.log
.DS_Store
.claude/settings.local.json
md/
CLAUDE.md
# Personal, environment-dependent (requires a local ComfyUI server) — never published.
skills/comfyui_image/
skills/ltx_video_faceid/
